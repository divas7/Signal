services:
  - type: web
    name: signal-backend
    env: python
    region: singapore
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000 --app-dir backend
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
